# R2 layout SVGs (authoring reference)

Symlinks (or copies) of `templates/decks/mblm-r2/templates/*.svg`.

Relative link depth from this folder: `../../../../templates/decks/mblm-r2/templates/<file>`.

In `mblm-ppt-master-step4-media-stack.zip` these are **real file copies** (Windows-friendly).
